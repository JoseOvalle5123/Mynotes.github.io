name: flutter_application_1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.7

dependencies:
  flutter:
    sdk: flutter

  # Dependencias existentes
  encrypt: ^5.0.3
  crypto: ^3.0.3
  cupertino_icons: ^1.0.8
  local_auth: ^2.3.0
  add_2_calendar: ^3.0.1

  # ── Firebase (BaaS) ──────────────────────────
  firebase_core: ^3.6.0          # Base obligatoria de Firebase
  firebase_auth: ^5.3.1          # Autenticación (BaaS)
  cloud_firestore: ^5.4.4        # Base de datos en la nube (Almacenamiento)
  firebase_messaging: ^15.1.3    # Notificaciones Push (FCM)
  flutter_local_notifications: ^17.2.2  # Mostrar notif en primer plano

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true